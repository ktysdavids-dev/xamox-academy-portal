# --- Base de datos (PostgreSQL en Render o local) ---
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"

# --- Auth.js / NextAuth ---
NEXTAUTH_SECRET="genera-un-secreto-largo-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# --- Stripe (Dashboard → Developers → API keys / Webhooks) ---
STRIPE_SECRET_KEY="sk_live_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# --- Administradores del campus (emails separados por coma, sin espacios problemáticos) ---
ADMIN_EMAILS="ktysdavids@gmail.com"

# --- URL pública del portal ---
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# --- Link de compra actual de Stripe (Payment Link de la cohorte) ---
NEXT_PUBLIC_STRIPE_PAYMENT_LINK="https://buy.stripe.com/bJedR894Nfosb9ZaL46Na00"

# --- Cuentas iniciales (se crean/actualizan en cada deploy si no existen) ---
BOOTSTRAP_ADMIN_EMAIL="ktysdavids@gmail.com"
BOOTSTRAP_ADMIN_PASSWORD="cambia-esto-por-una-clave-segura"
BOOTSTRAP_STUDENT_EMAIL="djxamo13@gmail.com"
BOOTSTRAP_STUDENT_PASSWORD="cambia-esto-por-una-clave-segura"

# --- Opcional: subida de archivos con Vercel Blob (PDF, imágenes, vídeos) ---
# Crea un store en https://vercel.com/storage/blob y pega el token de lectura/escritura.
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."
